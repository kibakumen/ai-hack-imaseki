# /dev の一時ファイル（ゲートの出力・差分・着手記録）
.dev/
node_modules/
.env
.env.*
.dev.vars
*.local

# 速成版（2026-09-20 に追跡から外した。種データの鍵が平文で入っていたため。手元には残っている）
sprint/

# デモ用の種データの本番SQL（パスワードのハッシュを含むので公開リポジトリへ入れない・2026-09-22）
web/.seed-demo-prod.sql
